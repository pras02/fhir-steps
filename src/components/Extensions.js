import React from 'react'

function Extensions(props) {
    return (
        <div>
            <p>{props.authURL}</p>
            <p>{props.tokenURL}</p>
        </div>
    )
}

export default Extensions
